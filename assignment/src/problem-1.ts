{
    // -----------------problem-01-----------------

    const sumArray=(arr:number[]):number=>{
        const sum=arr.reduce((sum,ele)=>sum+ele,0)
        return sum
    }
    // console.log(sumArray([5,5,6]));








    // end
}